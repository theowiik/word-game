# Test data source
spring.datasource.url=${SPRING_DATASOURCE_URL_TEST:jdbc:postgresql://localhost:5432/word_game_test}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME_TEST:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD_TEST:password}
# Drop tables before running tests
spring.liquibase.drop-first=true
